---
- name: Install common packages

  tasks:
    - name: Setup AUR
      import_playbook: setup_aur.yml

    - name: Install Neofetch
      yay:
        name: neofetch
        state: present
        executable: yay
